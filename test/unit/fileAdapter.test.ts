import BaseFileAdapter from './../../src/fileManage/baseFileAdapter';
import { FileRepository, FileInfo } from '../../src/model/fileModel';
import * as Sinon from 'sinon';
import * as chai from 'chai';

/*
class TestFileAdapter extends BaseFileAdapter {
  _download() {
  }

  _upload(file: FileInfo) {

  }

  _updateRemote(file: FileInfo) {

  }

  _deleteRemote(file: FileInfo) {

  }
}
var stub = Sinon.createStubInstance(BaseFileAdapter, {
  _upload: (file: FileInfo) => {
    return  Promise.resolve(0);
  }
});
*/

describe('#download', () => {

});
